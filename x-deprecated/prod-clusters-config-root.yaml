apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-clusters-root
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: app-clusters-config
    repoURL: https://github.com/knee-berts/multi-cluster-gke-argocd
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true