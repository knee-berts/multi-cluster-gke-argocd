apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argo-rollouts-root
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          env: "prod"
  template:
    metadata:
      name: '{{name}}-cluster-config'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/knee-berts/multi-cluster-gke-argocd/
        targetRevision: HEAD
        path: cluster-config/argo-rollouts
      destination:
        server: '{{server}}' # 'server' field of the secret
        namespace: argo-rollouts
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: true
        syncOptions:
          - CreateNamespace=true
